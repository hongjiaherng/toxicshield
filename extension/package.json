{
  "name": "toxicshield-extension",
  "version": "1.0.0",
  "description": "Chrome Extension for detecting toxicity in highlighted text",
  "scripts": {
    "build": "bun run build.ts",
    "build:prod": "bun --no-env-file --env-file=.env.production run build.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "@types/chrome": "^0.0.301",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.0.1"
  }
}
